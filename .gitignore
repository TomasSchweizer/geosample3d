# ----------------------------------------------------------------------
# Python Artifacts
# ----------------------------------------------------------------------

## Byte-compiled / optimized / DLL files
# Python automatically generates these for faster loading of modules.
# They are platform-dependent and should never be committed.
__pycache__/
*.py[cod]
*$py.class # Jython/other implementations

## Distribution / Packaging
# Files and directories created when building and packaging the project
# (e.g., source distributions, wheels, intermediate build files).
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
share/python-wheels/
*.egg-info/ # Metadata about the package
.installed.cfg
*.egg
MANIFEST

## PyInstaller
# Files generated by PyInstaller, used to create standalone executables.
# The manifest and spec file can often be re-generated or are used only
# during the build process.
*.manifest
*.spec

## Installer logs
# Logs generated by package managers like pip.
pip-log.txt
pip-delete-this-directory.txt

## Unit test / coverage reports
# Directories and files for test results and code coverage data.
# These are generated by testing frameworks (e.g., pytest, nose, coverage.py).
htmlcov/
.tox/ # tox (virtual environment management for testing)
.nox/ # nox (another test automation tool)
.coverage
.coverage.*
.cache
nosetests.xml
coverage.xml
*.cover
*.py,cover
.hypothesis/
.pytest_cache/ # pytest cache directory
cover/

## Translations
# Compiled and template files for internationalization (i18n).
*.mo # Compiled message files
*.pot # Portable object template

## Django stuff:
# Common files/artifacts generated during Django development.
*.log # General log files (often temporary)
local_settings.py # Often contains local, uncommitted configuration (like secrets/local database paths)
db.sqlite3 # Default development database file
db.sqlite3-journal # Temporary file for SQLite transactions

## Flask stuff:
# Common Flask artifacts.
instance/ # Holds instance-specific, non-version-controlled configuration and data
.webassets-cache # Cache for webassets

## Scrapy stuff:
# Artifacts generated by the Scrapy web crawling framework.
.scrapy

## Sphinx documentation
# Output build directories for documentation generated by Sphinx.
docs/_build/
doc/_build/

## PyBuilder
# Output directory for the PyBuilder build tool.
target/

## Jupyter Notebook
# Checkpoints created by Jupyter for versioning and recovery.
.ipynb_checkpoints

## IPython
# User-specific configuration and profiles.
profile_default/
ipython_config.py

## pyenv
# File specifying the local Python version managed by pyenv.
.python-version

## pipenv
# Note: Pipfile.lock is often RECOMMENDED for version control (uncomment to ignore).
# Ignoring it can cause dependency inconsistencies across platforms.
# Pipfile.lock

## PEP 582; used by e.g. pyflow
# Local packages directory as per PEP 582 proposal.
pypackages/

## Celery stuff
# Files used by the Celery distributed task queue manager.
celerybeat-schedule
celerybeat.pid # Process ID file

## SageMath parsed files
# Intermediate files generated by SageMath.
*.sage.py

## Environments
# Directories for Python virtual environments. These should always be ignored
# as they contain system-specific compiled code and are easily re-created.
.env # Generic environment file (often contains secrets, but sometimes committed if no secrets)
.venv # Common virtual environment directory name
env/
venv/
ENV/
env.bak/
venv.bak/

# ----------------------------------------------------------------------
# C++ / CMake Artifacts
# ----------------------------------------------------------------------

## Prerequisites
*.d

## Compiled Object files
# Intermediate files created by the compiler.
*.slo
*.lo
*.o
*.obj

## Precompiled Headers
# Intermediate files for faster compilation.
*.gch
*.pch

## Compiled Dynamic libraries
# Shared libraries, platform-specific and built from source.
*.so
*.dylib
*.dll

## Fortran module files
*.mod
*.smod

## Compiled Static libraries
# Archives of object files.
*.lai
*.la
*.a
*.lib

## Executables
# Compiled program binaries.
*.exe
*.out
*.app

## CMake
# Files generated by the CMake build system generator.
CMakeCache.txt
CMakeFiles/
CMakeScripts/
Makefile
cmake_install.cmake
install_manifest.txt
CTestTestfile.cmake
_deps/ # Submodule/dependency build directories

## IDE / Build Directories (CLion, Visual Studio, etc.)
# Directories created by IDEs for storing build artifacts, temporary files, etc.
cmake-build-debug/
cmake-build-release/
cmake-build-*/
.vs/ # Visual Studio specific directory
out/
x64/
x86/
Debug/
Release/

# ----------------------------------------------------------------------
# Pybind11 / Scikit-build Specifics
# ----------------------------------------------------------------------

## Generated Python Extension modules (in-place builds)
# Binary extension modules built from C/C++ code, platform-specific.
*.pyd
*.so
*.dylib

## Local build directories often used by scikit-build
_skbuild/

## Cibuildwheel output directory
# Directory for platform-specific wheel files generated for distribution.
wheelhouse/

# ----------------------------------------------------------------------
# Editors & OS
# ----------------------------------------------------------------------

## VS Code
# VS Code workspace files (can be optionally committed if collaboration is involved).
.vscode/
# Allow the specific files below to be tracked if needed for shared workspace config:
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/launch.json
!.vscode/extensions.json
*.code-workspace

## IntelliJ / PyCharm / CLion
# IDE configuration directory.
.idea/

## Mac
# OS X generated files.
.DS_Store
.AppleDouble
.LSOverride

## Windows
# OS generated files.
Thumbs.db
ehthumbs.db
Desktop.ini

## Linux
# Temporary backup files created by various editors (e.g., Vim, Emacs).
*~

# ----------------------------------------------------------------------
# Data Files (Optional - Modify as needed)
# ----------------------------------------------------------------------

# Exclude large data files often used in testing (e.g., point clouds, large datasets).
# Uncomment or modify this section based on your project's specific data storage.
# *.ply
# *.pcd
# *.h5
# *.las
# *.laz
# *.xyzn
# *.xyz
# *.bin